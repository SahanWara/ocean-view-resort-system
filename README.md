# ocean-view-resort-system
The Ocean View Resort Reservation System is a web-based application developed to automate hotel reservation management. It allows users to log in, manage reservations, calculate bills, and generate reports. The system uses a Spring Boot REST API and MySQL database, providing a scalable and user-friendly solution to replace manual booking processes.
